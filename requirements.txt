# --- core data ---
pandas>=2.2
numpy>=1.26
openpyxl>=3.1            # to read the .xlsx dataset
pyarrow>=15              # parquet
tqdm>=4.66
python-dotenv>=1.0
pydantic>=2.7
pydantic-settings>=2.3

# --- LLM + embeddings (provider-agnostic; works for OpenAI, Groq,
# --- Together, Fireworks, OpenRouter, HuggingFace TGI, and Databricks
# --- Foundation Model serving via OPENAI_BASE_URL).
openai>=1.40
tiktoken>=0.7

# --- vector search (local default) ---
faiss-cpu>=1.8

# --- web search / validation ---
tavily-python>=0.3

# --- API server ---
fastapi>=0.110
uvicorn[standard]>=0.30

# --- tracing ---
mlflow>=2.13

# --- dev / nice-to-haves ---
rich>=13.7
huggingface_hub>=0.20

# --- OPTIONAL: Mosaic AI Vector Search backend (Databricks). The
# --- FastAPI retriever lazy-imports this only when VECTOR_SEARCH_ENDPOINT
# --- is set, so HF Spaces deploys can leave it commented out to keep
# --- the image slim. Install with:
# ---     pip install databricks-vectorsearch
# databricks-vectorsearch>=0.40

# --- OPTIONAL: Databricks SDK + MLflow integrations (only needed when
# --- running the databricks/notebooks/* paths from outside Databricks).
# databricks-sdk>=0.30
# mlflow-skinny[databricks]>=2.13
